<?xml version="1.0" encoding="utf-8"?>
<resources>

  <style name="Theme.Blackjackai_test" parent="android:Theme.Material.Light.NoActionBar" />

  <style name="Theme.Blackjack" parent="Theme.Material3.DayNight.NoActionBar" />

  <style name="Theme.WebViewFull" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- 상태바/내비바를 투명하게 -->
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <!-- (API 28+) 노치 영역까지 사용 -->
    <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    <!-- (선택) 내비바 구분선 제거 -->
    <item name="android:navigationBarDividerColor">@android:color/transparent</item>
  </style>

</resources>
